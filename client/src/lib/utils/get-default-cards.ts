import { SlugIds } from "../target/heros";

export function getDefaultCards(heroId: SlugIds) {
  let set = getCardSet(heroId, 'classic')
  set.
}


export function getCardSet(slugId: SlugIds, setName: string) {
  
}